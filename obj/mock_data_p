from faker import Faker

def generate_customer(self, qtd):

    return [
        {
            "cpf": self.fake.cpf().replace('.', '').replace('-', ''),
            "name": self.fake.name(),
            "address": self.fake.address(),
            "email": self.fake.email()
        }
        for _ in range(qtd)
    ]